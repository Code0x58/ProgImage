# dependencies are managed in the Pipfile, this is to allow the app to be installed within the virtualenv
from setuptools import find_packages, setup

setup(name="ProgImageService", packages=find_packages())
